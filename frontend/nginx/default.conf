limit_req_zone $binary_remote_addr zone=one:10m rate=20r/s;

server {
    listen 80;
    server_name localhost;

    # -----------------------
    # Security headers
    # -----------------------
   

    client_header_timeout 10s;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # -----------------------
    # GZIP
    # -----------------------
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types 
        text/plain 
        text/css 
        application/javascript 
        application/json 
        application/xml 
        image/svg+xml;

    client_max_body_size 50M;

    # -----------------------
    # Next.js (Frontend)
    # -----------------------
    location / {
        proxy_pass http://187.127.117.38:3000;
        proxy_http_version 1.1;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;

        proxy_cache_bypass $http_upgrade;
    }

    # -----------------------
    # API Backend
    # -----------------------
    location /api/ {
        limit_req zone=one burst=20 nodelay;
        proxy_pass http://187.127.117.38:8000/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        limit_except GET POST PUT DELETE PATCH {
            deny all;
        }
    }

    location /auth/ {
        limit_req zone=one burst=20 nodelay;
        proxy_pass http://187.127.117.38:8000;
        proxy_http_version 1.1;

        limit_except GET POST PUT DELETE PATCH {
            deny all;
        }
    }

    # -----------------------
    # Badminton Service
    # -----------------------
    # -----------------------
    # MinIO Service (Badminton Storage)
    # -----------------------
    location /badminton/ {
    # rewrite เพื่อเติมชื่อ bucket เข้าไปใน path ก่อนส่งให้ MinIO
    # จาก /badminton/image.png กลายเป็น /badminton/badminton/image.png (ถ้า bucket ชื่อ badminton)
    # หรือถ้า URL มาเป็น /badminton/file อยู่แล้ว ให้ใช้ rewrite ดังนี้:
    rewrite ^/badminton/(.*)$ /badminton/$1 break;

    proxy_pass http://187.127.117.38:9100;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_http_version 1.1;
    proxy_connect_timeout 300s;
    chunked_transfer_encoding off;
    proxy_buffering off;
}

    



    # -----------------------
    # Next.js static optimization
    # -----------------------
    location /_next/static/ {
        proxy_pass http://187.127.117.38:3000/_next/static/;
        expires 6M;
        access_log off;
        add_header Cache-Control "public, max-age=15552000, immutable";
    }

    # -----------------------
    # Error pages
    # -----------------------
    error_page 500 502 503 504 /50x.html;

    location = /50x.html {
        root /usr/share/nginx/html;
    }
}